name: MyYml

on: 
   push:
      branches: [ "main" ]
  
jobs:
  build_docker_and_execute:
      name: Build and execute dockerfile
      runs-on: self-hosted
      
      steps:
      - name: Build docker
        run: |
          docker ps
